#include <iomanip>
#include <iostream>
using namespace std;

int main()
{
    int n,i,s=0;
    cin>>n;
    double a[n],c=0;
    for(i=0;i<n;i++)
    cin>>a[i];
    
    for(i=0;i<n;i++){
        if(a[i]>0) {
        s++;
        c=c+a[i];
        }
     
    }
    if(c>0) cout<<fixed<<setprecision(2)<<c/s;
    else cout<<"Not Found";
}
