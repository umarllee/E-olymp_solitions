#include <iostream>
using namespace std;
 
int main() {
    
    double a, b, x, y, z;
    cin >> a >> b >> x >> y >> z;
    if ((x < a && y < b) || (y < a && x < b) || (y < a && z < b) || 
    (z < a && y < b) || (x < a && z < b) || (z < a && x < b)) {
        cout << 1;
    } else {
        cout << 0;
    }
 
}
